---
title: Reverse Engineering — Berlin based party and record label
layout: 'base.njk'
description: International engineering conglomerates aren't typically known for their partying sensibilities but there was a gap in the market and we filled our (metal tipped) boots. Anyway we’re a collective of artists who are quite earnestly trying to make fun events that we want to go to.

---

<div style="padding:8px">
<h1>Reverse Engineering</h1>
<p><a href="/vault" style="color:white;padding:0px;">Access vault (1 item)</a></p>
</div>


{% if page.url == "/vault/" %}
<div class="vault">
  <h2>Welcome to the Vault</h2>
  <p>This is a restricted area for administrators only. Please ensure you have the proper credentials to access this section.</p>
</div>
{% endif %}

<ul class="tree">
  <li>
    <details open>
      <summary><h1>Events</h1></summary>
      <ul>
        {% for events in collections.events | reverse %}
        <li>
          <details>
            <summary><h2>{{ events.data.title }}</h2></summary>
            <ul>
              <li><div class="list-date"><h3>{{ events.data.date | postDate }}</h3></div></li>
              <li><div class="list-content">{{ events.templateContent | safe }}</div></li>
            </ul>
          </details>
        </li>
        {% endfor %}
      </ul>
    </details>
  </li>
  <li>
    <details open>
      <summary><h2>Releases</h2></summary>
      <ul>
        {% for releases in collections.releases | reverse %}
        <li>
          <details>
            <summary><h3>{{ releases.data.title }}</h3></summary>
            <ul>
              <!-- <li><div class="list-date">{{ releases.data.date }}</div></li> -->
              <li><div class="list-content">{{ releases.templateContent | safe }}</div></li>
            </ul>
          </details>
        </li>
        {% endfor %}
      </ul>
    </details>
  </li>
  <li>
    <details open>
      <summary><h2>Hardware</h2></summary>
      <ul>
        {% for hardware in collections.hardware | reverse %}
        <li>
          <details>
            <summary><h3>{{ hardware.data.title }}</h3></summary>
            <ul>
              <li><div class="list-content">{{ hardware.templateContent | safe }}</div></li>
            </ul>
          </details>
        </li>
        {% endfor %}
      </ul>
    </details>
  </li>
  <li>
    <details open>
      <summary><h2>Recordings</h2></summary>
      <ul>
        {% for recordings in collections.recordings | reverse %}
        <li>
          <details>
            <summary><h3>{{ recordings.data.date | postDate }} / {{ recordings.data.title }}</h3></summary>
            <ul>
              <!-- <li><div class="list-date">{{ recordings.data.date | postDate }}</div></li> -->
              <li><div class="list-content">{{ recordings.templateContent | safe }}</div></li>
            </ul>
          </details>
        </li>
        {% endfor %}
      </ul>
    </details>
  </li>
  <li>
    <details open>
      <summary><h2>links</h2></summary>
      <ul>
        {% for links in collections.links %}
        <li>
          <details>
            <summary><h3>{{ links.data.title }}</h3></summary>
            <ul>
              <li><div class="list-content">{{ links.templateContent | safe }}</div></li>
            </ul>
          </details>
        </li>
        {% endfor %}
      </ul>
    </details>
  </li>
</ul>








